const $=id=>document.getElementById(id);
let session=null,busy=false,currentTab='users',generated='',newKey='';
const pages={codes:0,users:0,audit:0,issues:0},more={};
let siteRevision=0;
const when=t=>t?new Date(t*1000).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai',hour12:false}):'未开通';
const node=(tag,text,cls)=>{const e=document.createElement(tag);if(text!==undefined)e.textContent=text;if(cls)e.className=cls;return e;};
const notice=createNotice($('admin-notice'));
async function api(path,data){const r=await fetch('/api/zhuixins_x/'+path,{method:data===undefined?'GET':'POST',credentials:'same-origin',headers:data===undefined?{}:{'Content-Type':'application/json','X-CSRF-Token':session?.csrf||''},body:data===undefined?undefined:JSON.stringify(data)});let body;try{body=await r.json();}catch{throw new Error('后台响应异常，请重试。');}if(!r.ok)throw new Error(body.error||'操作失败');return body;}
async function action(fn){if(busy)return;busy=true;document.querySelectorAll('button:not(.notice-close)').forEach(b=>b.disabled=true);try{await fn();}catch(e){notice(e.message,true);}finally{busy=false;document.querySelectorAll('button:not(.notice-close)').forEach(b=>b.disabled=false);}}
function download(name,text){const url=URL.createObjectURL(new Blob([text],{type:'text/plain;charset=utf-8'}));const a=node('a');a.href=url;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
async function load(){session=await api('status');$('admin-login').hidden=session.signed_in;$('admin-workspace').hidden=!session.signed_in;$('admin-logout').hidden=!session.signed_in;if(session.signed_in){await showTab(currentTab);}else{$('generated-panel').hidden=true;generated='';$('generated-codes').value='';$('codes-list').replaceChildren();$('users-list').replaceChildren();$('audit-list').replaceChildren();$('user-detail').replaceChildren();$('user-detail').hidden=true;$('issues-list').replaceChildren();}}
async function overview(){const d=await api('overview');const box=$('admin-stats');box.replaceChildren();for(const [label,value] of [['用户总数',d.users.total],['使用期有效',d.users.active],['停用用户',d.users.suspended],['可兑换激活码',d.codes.available]]){const item=node('div');item.append(node('strong',String(value)),node('span',label));box.append(item);}}
const adminViews={users:['用户管理','查看用户状态，管理使用期与权限。'],codes:['激活码','生成、分发与追踪每一次开通和续期。'],issues:['异常中心','集中排查任务异常，记录处理结果。'],site:['站点设置','管理网站信息、新用户注册与赠送策略。'],audit:['操作记录','追踪管理操作、用户兑换与注册赠送。'],security:['管理安全','管理后台访问凭据。']};
async function showTab(name){await overview();$('admin-page-title').textContent=adminViews[name][0];$('admin-page-description').textContent=adminViews[name][1];currentTab=name;document.querySelectorAll('.admin-tab').forEach(e=>e.hidden=e.id!=='tab-'+name);document.querySelectorAll('[data-tab]').forEach(e=>e.setAttribute('aria-current',e.dataset.tab===name?'page':'false'));if(name!=='security')await loaders[name]();}
function pagination(name,data){more[name]=data.has_more;$(name+'-prev').hidden=pages[name]===0;$(name+'-next').hidden=!data.has_more;$(name+'-page').textContent='第 '+(pages[name]+1)+' 页';}
async function codes(){const d=await api('codes?'+new URLSearchParams({page:pages.codes,status:$('codes-filter').value}));pagination('codes',d);const box=$('codes-list');box.replaceChildren();if(!d.codes.length)box.textContent='没有符合条件的激活码。';for(const c of d.codes){const row=node('article',undefined,'admin-row');const status=c.redeemed_by?'已兑换':c.disabled?'已停用':c.valid_until&&c.valid_until<=Date.now()/1000?'已过兑换期限':'可兑换';row.append(node('h3',c.hint+' · '+c.duration_days+' 天'),node('p',status+' · '+(c.note||'无备注'),'record-result'),node('p','生成：'+when(c.created_at)+' · 最晚兑换：'+(c.valid_until?when(c.valid_until):'不限'),'hint'));if(c.redeemed_by)row.append(node('p','兑换用户：'+c.redeemed_by+' · '+when(c.redeemed_at),'hint'));const reveal=node('button','显示完整激活码','text-button');reveal.onclick=()=>action(async()=>{const r=await api('codes/reveal',{id:c.id});const value=node('input');value.readOnly=true;value.value=r.code;value.setAttribute('aria-label','完整激活码');row.append(value);reveal.hidden=true;});row.append(reveal);if(!c.redeemed_by&&!c.disabled){const revoke=node('button','停用此码','danger-button');revoke.onclick=()=>action(async()=>{await api('codes/revoke',{id:c.id});await codes();await overview();notice('激活码已停用。');});row.append(revoke);}box.append(row);}}
function field(label,type,value){const wrap=node('label',label);const input=node('input');input.type=type;input.value=value;wrap.append(input);return {wrap,input};}
async function users(){const d=await api('users?'+new URLSearchParams({page:pages.users,search:$('user-search').value}));pagination('users',d);const box=$('users-list');box.replaceChildren();if(!d.users.length)box.textContent='没有符合条件的用户。';for(const u of d.users){const row=node('article',undefined,'admin-row');row.append(node('h3',u.label||'已删除本站资料'),node('p','用户编号：'+u.account_id,'hint'),node('p',(u.suspended?'已停用':u.expires_at>Date.now()/1000?'使用期有效':'待激活 / 已到期')+' · 到期：'+when(u.expires_at),'record-result'));const form=node('form',undefined,'admin-user-form');const days=field('追加天数','number','7');days.input.min=1;days.input.max=3650;const expiry=field('指定到期时间（北京时间）','datetime-local',u.expires_at?new Date(u.expires_at*1000+28800000).toISOString().slice(0,16):'');const reason=field('操作备注','text','');reason.input.maxLength=200;form.append(days.wrap,expiry.wrap,reason.wrap);const update=async(mode)=>{const data={id:u.account_id,revision:u.revision,mode,reason:reason.input.value};if(mode==='extend')data.days=days.input.value;if(mode==='expiry'){if(!expiry.input.value)throw new Error('请指定到期时间。');data.expires_at=Math.floor(Date.parse(expiry.input.value+'+08:00')/1000);}await api('users/update',data);await users();await overview();if(!$('user-detail').hidden&&$('user-detail').dataset.accountId===u.account_id)await userDetail(u.account_id);notice('用户权限已更新。');};for(const [label,mode] of [['追加时长','extend'],['设置到期时间','expiry'],[u.suspended?'恢复用户':'停用用户',u.suspended?'resume':'suspend']]){const b=node('button',label,mode==='suspend'?'danger-button':'text-button');b.type='button';b.onclick=()=>action(()=>update(mode));form.append(b);}const detailButton=node('button','查看用户详情','text-button');detailButton.type='button';detailButton.onclick=()=>action(()=>userDetail(u.account_id));const controls=node('details',undefined,'user-controls');controls.append(node('summary','调整使用期与权限'),form);row.append(detailButton,controls);box.append(row);}}
const actionNames={registration_gift:'新用户注册赠送',site_update:'更新站点设置',user_note:'修改用户备注',issue_review:'处理异常记录',login:'后台登录',create_codes:'生成激活码',reveal_code:'查看激活码',revoke_code:'停用激活码',redeem:'用户兑换',user_extend:'追加用户时长',user_expiry:'设置到期时间',user_suspend:'停用用户',user_resume:'恢复用户',rotate_key:'更换管理密钥'};
async function audit(){const d=await api('audit?page='+pages.audit);pagination('audit',d);const box=$('audit-list');box.replaceChildren();if(!d.events.length)box.textContent='暂无操作记录。';for(const e of d.events){const row=node('article',undefined,'admin-row');row.append(node('h3',(actionNames[e.action]||e.action)+' · '+when(e.created_at)),node('p','操作者：'+(e.actor==='admin'?'管理员':e.actor==='system'?'系统':'用户')+' · 对象：'+e.target,'hint'));const detail=node('details');detail.append(node('summary','查看操作明细'));const pre=node('pre');try{pre.textContent=JSON.stringify(JSON.parse(e.details),null,2);}catch{pre.textContent='明细不可用';}detail.append(pre);row.append(detail);box.append(row);}}
const loaders={codes,users,audit,issues,site};
$('admin-login-form').onsubmit=e=>{e.preventDefault();action(async()=>{const key=$('admin-key').value;$('admin-key').value='';await api('login',{key});await load();notice('已登录管理后台。');});};
$('admin-logout').onclick=()=>action(async()=>{await api('logout',{});await load();notice('已退出管理后台。');});
document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>action(()=>showTab(b.dataset.tab)));
$('create-codes-form').onsubmit=e=>{e.preventDefault();action(async()=>{const valid=$('code-valid-until').value;const r=await api('codes/create',{days:$('code-days').value,count:$('code-count').value,note:$('code-note').value,valid_until:valid?Math.floor(Date.parse(valid+'+08:00')/1000):null});generated=r.codes.map(c=>c.code+' | '+c.days+' 天').join('\n');$('generated-codes').value=generated;$('generated-panel').hidden=false;pages.codes=0;await codes();await overview();notice('已生成 '+r.codes.length+' 个激活码。可下载后分发给用户。');});};
$('download-codes').onclick=()=>download('轻步云-activation-codes.txt',generated);
$('codes-filter').onchange=()=>action(async()=>{pages.codes=0;await codes();});
$('refresh-codes').onclick=()=>action(codes);$('refresh-audit').onclick=()=>action(audit);
$('search-users').onsubmit=e=>{e.preventDefault();action(async()=>{pages.users=0;await users();});};
for(const name of ['codes','users','audit','issues'])for(const direction of ['prev','next'])$(name+'-'+direction).onclick=()=>action(async()=>{if(direction==='prev'&&pages[name]>0)pages[name]--;if(direction==='next'&&more[name])pages[name]++;await loaders[name]();});
$('rotate-key').onclick=()=>action(async()=>{if(!$('rotate-confirm').checked)throw new Error('请先勾选更换密钥确认。');const r=await api('rotate-key',{confirm:true});newKey=r.key;$('new-key').value=newKey;$('new-key-panel').hidden=false;await load();notice('密钥已更换。请保存新密钥，旧密钥已经失效。');});
$('download-key').onclick=()=>download('轻步云-admin-key.txt',newKey);

function giftPreview(){const n=Number($('site-gift-days').value),open=$('site-registration').checked,valid=$('site-gift-days').value!==''&&Number.isSafeInteger(n)&&n>=0&&n<=3650;$('gift-preview-title').textContent=!open?'新用户注册已关闭':!valid?'请填写有效天数':n>0?'首次注册赠送 '+n+' 天':'新用户不赠送使用期';$('gift-preview-description').textContent=!open?'当前仅已有用户可登录。开放注册后按设置的天数赠送。':!valid?'请输入 0–3650 的整数。':n>0?'验证 Zepp 账号并首次注册成功后，自动开通使用期。':'注册后需兑换激活码开通。';}
$('site-gift-days').oninput=giftPreview;$('site-registration').onchange=giftPreview;
async function site(){const d=await api('site');siteRevision=d.revision;$('site-name').value=d.name;$('site-notice').value=d.announcement;$('site-support').value=d.contact;$('site-registration').checked=d.registration_open;$('site-gift-days').value=d.new_user_gift_days;giftPreview();$('site-version').textContent='最近更新 '+when(d.updated_at)+' · 版本 '+d.revision;}
$('site-form').onsubmit=e=>{e.preventDefault();action(async()=>{await api('site',{name:$('site-name').value,announcement:$('site-notice').value,contact:$('site-support').value,registration_open:$('site-registration').checked,new_user_gift_days:Number($('site-gift-days').value),revision:siteRevision});await site();notice('站点设置已保存，用户刷新页面后生效。');});};
const taskStatus={pending:'等待排队',queued:'排队中',running:'执行中',success:'接口已接收',failed:'失败',unknown:'结果待确认',skipped:'已跳过'};
const taskKind={manual:'手动执行',schedule:'自动执行',check:'连接测试',verify:'只读核对'};
const stepText=n=>n===null||n===undefined?'未读取':Number(n).toLocaleString()+' 步';
function detailRows(box,title,items){const section=node('section',undefined,'detail-section');section.append(node('h3',title));if(!items.length)section.append(node('p','暂无记录','hint'));for(const text of items)section.append(node('p',text,'detail-line'));box.append(section);}
async function userDetail(id){
 const d=await api('users/detail?'+new URLSearchParams({id})),box=$('user-detail');box.replaceChildren();box.hidden=false;box.dataset.accountId=id;
 const head=node('div',undefined,'card-heading');head.append(node('h2',d.account?.label||'已删除账号资料'));const close=node('button','收起详情','text-button');close.type='button';close.onclick=()=>{box.hidden=true;};head.append(close);box.append(head,node('p','用户编号：'+id,'hint'));
 const grid=node('div',undefined,'record-details');for(const [label,value] of [['使用期',d.access.suspended?'已停用':d.access.active?'有效至 '+when(d.access.expires_at):'未激活或已到期'],['注册时间',when(d.member.created_at)],['每日范围',d.account?stepText(d.account.min_step)+' – '+stepText(d.account.max_step):'资料已删除'],['自动计划',d.account?.enabled?'已开启':'已暂停'],['登录凭据',!d.account?'已删除':d.account.needs_login?'需重新登录':'未标记失效'],['预计下次执行',d.runtime.next.at?when(d.runtime.next.at):d.runtime.next.reason],['最近云端读数',d.runtime.reading?stepText(d.runtime.reading.observed_step)+' · 数据日期 '+d.runtime.reading.day+' · 读取于 '+when(d.runtime.reading.checked_at):'尚未读取']]){const cell=node('div');cell.append(node('span',label),node('strong',value));grid.append(cell);}box.append(grid);
 const form=node('form');const label=node('label','内部备注（仅管理员可见）'),input=node('textarea');input.rows=3;input.maxLength=600;input.value=d.note.note;label.append(input);const save=node('button','保存备注','primary');save.type='submit';form.append(label,save);form.onsubmit=e=>{e.preventDefault();action(async()=>{await api('users/note',{id,note:input.value,revision:d.note.revision});await userDetail(id);notice('用户备注已保存。');});};box.append(form);
 detailRows(box,'近期执行 · 最多 30 条',d.runs.map(r=>when(r.created_at)+' · '+taskKind[r.kind]+' · '+(r.verification==='matched'?'云端达到目标':r.verification==='waiting'?'等待自动核对':taskStatus[r.status])+' · '+r.message+' · 读数 '+stepText(r.observed_step)));
 detailRows(box,'近期兑换 · 最多 20 条',d.redemptions.map(r=>when(r.created_at)+' · '+r.hint+' · '+r.duration_days+' 天 · 兑换后到期 '+when(r.new_expiry)));
 detailRows(box,'近期用户操作 · 最多 20 条',d.events.map(r=>when(r.created_at)+' · '+(actionNames[r.action]||r.action)));
 box.scrollIntoView({behavior:'smooth',block:'start'});
}
async function issues(){
 const d=await api('issues?'+new URLSearchParams({page:pages.issues,filter:$('issues-filter').value}));pagination('issues',d);
 const counts=$('issue-counts');counts.replaceChildren();for(const [label,n] of [['待处理',d.counts.open],['凭据失效',d.counts.credentials],['已处理',d.counts.handled]]){const item=node('div');item.append(node('strong',String(n)),node('span',label));counts.append(item);}
 const box=$('issues-list');box.replaceChildren();if(!d.issues.length){box.append(node('p','当前没有符合条件的异常。','hint'));return;}
 const labels={credentials:'凭据失效',unknown:'提交结果待确认',delayed:'任务超过 15 分钟未更新',failed:'执行失败',unverified:'云端读数未核实'};
 for(const r of d.issues){const row=node('article',undefined,'admin-row issue-row');row.append(node('h3',labels[r.category]+' · '+r.label),node('p',r.message,'record-result'),node('p',r.advice,'issue-advice'),node('p','更新 '+when(r.updated_at)+(r.day?' · 数据日期 '+r.day:'')+' · '+(r.handled?'已处理':'待处理'),'hint'));
 const detail=node('button','查看用户详情','text-button');detail.onclick=()=>action(async()=>{await showTab('users');await userDetail(r.account_id);});row.append(detail);
 if(r.review_note)row.append(node('p','上次处理意见：'+r.review_note+' · '+when(r.reviewed_at),'hint'));
 if(r.category!=='credentials'&&!r.handled){const form=node('form',undefined,'issue-review-form');const label=node('label','处理意见'),note=node('textarea');note.maxLength=600;note.rows=2;note.required=true;label.append(note);const button=node('button','记录处理结果','secondary-button');button.type='submit';form.append(label,button);form.onsubmit=e=>{e.preventDefault();action(async()=>{await api('issues/review',{id:r.id,stamp:r.stamp,revision:r.review_revision,note:note.value});await issues();notice('已记录处理意见；没有重新执行或修改原任务结果。');});};row.append(form);}
 box.append(row);
 }
}
$('refresh-issues').onclick=()=>action(issues);
$('issues-filter').onchange=()=>action(async()=>{pages.issues=0;await issues();});

load().catch(e=>notice(e.message,true));
